// import server from '../../../server';
// import supertest from 'supertest';

// const request = supertest(server);

describe('DataTable', () => {
  test('fetch draft DataTables', () => {
    expect(true).toBeTruthy()
  })
});
